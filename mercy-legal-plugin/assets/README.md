# Mercy Legal Assets

The Office manifest currently references PNG ribbon icons at `assets/icon-16.png`, `assets/icon-32.png`, `assets/icon-64.png`, and `assets/icon-80.png`.

Use `assets/icon.svg` as the source artwork when exporting the final icon set. The SVG is intentionally simple so it remains legible in the Word ribbon.
